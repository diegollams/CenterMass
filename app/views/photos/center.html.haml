%h2
  Superposición de fotos
  %p.lead
    *Seleccione las imágenes que desea superponer y presione el botón de 'Superponer'
= simple_form_for(@photo,url: center_photo_post_new_photo_path	) do |f|
  = f.error_notification

  .form-inputs
    = f.input :photos,collection: @photos.collect{ |p| [(display_image p.image,3,p.x_center_mass,p.y_center_mass), p.id] },:as => :check_boxes

  .form-actions
    = f.button :submit, "Superponer"

