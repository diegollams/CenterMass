%p.lead
  *Agregar imagen binarizada convertida a texto, separar con salto de línea cada renglón.
= simple_form_for(@photo) do |f|
  = f.error_notification

  .form-inputs
    = f.input :image, :input_html => {:rows => 30}

  .form-actions
    = f.button :submit, 'Agregar imagen'
